# Ignore everything by default
*

# Allow .gitignore itself
!.gitignore

# Allow directories so .r files inside them are visible
!*/

# Allow .r files (both .r and .R)
!*.[rR]

# Still ignore the UNUSED folder
scripts/UNUSED/
